import { ObserveFilesDirective } from './observe-files.directive';

describe('ObserveFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveFilesDirective();
    expect(directive).toBeTruthy();
  });
});
